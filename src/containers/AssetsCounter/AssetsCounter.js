import React, { Component } from 'react';

import Counter from '../../components/Counter/Counter';

export default class AssetsCounter extends Component {
  render() {
    return (
      <div>
        <Counter />
      </div>
    );
  }
}
